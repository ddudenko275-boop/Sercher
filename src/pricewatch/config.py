"""Конфигурация монитора цен.

Пока задача — де-риск сборщика Авито, поэтому здесь только то, что нужно
сборщику. Жёсткие фильтры, Telegram и модель добавим, когда данные пойдут.

Идея по фильтрам: жёсткие условия (цена, город, категория, ключевые слова)
задаёт сам Авито через URL сохранённого поиска — ты настраиваешь фильтры в
браузере и вставляешь готовую ссылку сюда. Наш код тогда просто читает свежие
результаты, а не пытается воспроизвести фильтры Авито.
"""

from __future__ import annotations

import os
from pathlib import Path


def _load_dotenv() -> None:
    """Подтянуть переменные из .env в корне проекта (без сторонних библиотек).

    Нужно, чтобы токен Telegram подхватывался и при запуске из Планировщика,
    где переменные окружения не заданы. Существующее окружение имеет приоритет.
    """
    env_path = Path(__file__).resolve().parents[2] / ".env"
    if not env_path.exists():
        return
    for line in env_path.read_text(encoding="utf-8").splitlines():
        line = line.strip()
        if not line or line.startswith("#") or "=" not in line:
            continue
        key, val = line.split("=", 1)
        os.environ.setdefault(key.strip(), val.strip())


_load_dotenv()

# --- Что отслеживаем: категория «Часы и украшения», запрос «золото 585» ---
# {region} — слуг региона Авито. s=104 — «по дате: сначала новые».
SEARCH_URL_TEMPLATE = "https://www.avito.ru/{region}/chasy_i_ukrasheniya?q=золото+585&s=104"

# Кольца близости к Ростову/Москве (Европейская часть), от ближних к дальним.
# Кольцо 0 — крупнейшие/быстрые рынки, проверяется КАЖДЫЙ проход. Дальние кольца
# проверяются по очереди между проходами. {человекочитаемое имя: слуг Авито}.
REGION_RINGS = [
    {  # 0 — крупнейшие рынки, проверяются КАЖДЫЙ проход наравне с Ростовом
        "Ростовская область": "rostovskaya_oblast",
        "Москва": "moskva",
        "Московская область": "moskovskaya_oblast",
        "Санкт-Петербург": "sankt-peterburg",
        "Краснодарский край": "krasnodarskiy_kray",
    },
    {  # 1 — соседние с Ростовом
        "Волгоградская область": "volgogradskaya_oblast",
        "Воронежская область": "voronezhskaya_oblast",
        "Ставропольский край": "stavropolskiy_kray",
        "Калмыкия": "respublika_kalmykiya",
    },
    {  # 2 — юг/центр пошире
        "Астраханская область": "astrahanskaya_oblast",
        "Белгородская область": "belgorodskaya_oblast",
        "Липецкая область": "lipetskaya_oblast",
        "Саратовская область": "saratovskaya_oblast",
        "Крым": "respublika_krym",
        "Севастополь": "sevastopol",
        "Тамбовская область": "tambovskaya_oblast",
        "Пензенская область": "penzenskaya_oblast",
        "Курская область": "kurskaya_oblast",
    },
    {  # 3 — остальная Европейская часть (крупные)
        "Нижегородская область": "nizhegorodskaya_oblast",
        "Самарская область": "samarskaya_oblast",
        "Татарстан": "respublika_tatarstan",
        "Ленинградская область": "leningradskaya_oblast",
    },
]

# Обычный проход: несколько страниц новейших (сортировка по дате) — с запасом
# ловим всё новое между проходами, но не листаем весь рынок каждые 15 мин.
PAGES = 3
# Первый прогон чуть глубже — захватить уже висящие предложения.
FIRST_RUN_PAGES = 3
# Глубокий прочёс ВСЕГО рынка (все страницы региона, круг за кругом). Включается
# переменной окружения PRICEWATCH_DEEP=1 для разового полного сканирования.
# Листаем до пустой страницы, но не глубже этого потолка. Реальный предел Avito API
# ~100 страниц (~3000–5000 объявлений/регион), глубже он отдаёт HTTP 400. Ставим 90
# (≈2700+/регион вместо прежних 750). Больше страниц = дольше и больше блоков.
MAX_PAGES_DEEP = 90
DEEP_SWEEP = bool(os.getenv("PRICEWATCH_DEEP"))

# Ценовые полосы для глубокого прочёса — обход лимита Авито (~5000 объявлений на
# запрос; в одном регионе бывает 60k+). Один большой запрос режем на полосы по цене,
# каждая влезает в лимит, листаем целиком, объединяем → почти полный охват.
# Границы ВЫВЕДЕНЫ из порогов фильтра, поэтому годное не теряется:
#   • лом (в цене — ₽/грамм): годный ₽/г ∈ [MIN_PLAUSIBLE=1000, MAX=8300] → полоса [1000,9000];
#   • обычное (в цене — полная сумма): ≥35 г дешевле 8300 ₽/г ⇒ цена ≥ 35×1000 = 35k ⇒ полосы от 30k;
#   • дыра 9k–30k заведомо без годного (там либо мусор <1000 ₽/г, либо ₽/г>8300).
# Каждое УВИДЕННОЕ объявление всё равно проходит полный фильтр — цена лишь маршрутизирует.
# Формат: (pmin, pmax, макс_страниц). «Умный компромисс»: где живут тяжёлые изделия
# (дорогие полосы) и лом — листаем ГЛУБОКО; огромную дешёвую массу 30–100k (мелочёвка,
# редко ≥35 г) — только ПО ВЕРХАМ (свежее). Так ~99% находок за ~10% запросов/банов.
# Порядок = приоритет сбора. None = без верхней границы.
PRICE_BANDS = [
    (1_000, 9_000, 90),        # ЛОМ (цена = ₽/грамм, ~5000–8500) — ГЛАВНЫЙ приоритет:
                               # сюда попадают «цепь 50 г за 7500» (7500 = ₽/г). Первой и глубоко.
    (100_000, 200_000, 90),    # тяжёлые изделия с полной ценой
    (200_000, 400_000, 90),
    (400_000, None, 90),
    (30_000, 100_000, 12),     # дешёвая масса (мелочёвка, редко ≥35 г): только верхушка
]
# Файл прогресса свипа (регион×полоса) — рестарт продолжает, а не пережигает страницы.
SWEEP_PROGRESS_PATH = "data/sweep_progress.json"

# Пауза между отправками в Telegram — чтобы сотни находок за свип НЕ упёрлись во
# флуд-лимит и не привели к блокировке бота. ~1/сек на чат безопасно.
TELEGRAM_SEND_PACE_SEC = 1.2

# «Живой» человеческий темп, чтобы Авито не банил IP прокси. Случайные паузы
# (секунды) — обход всех регионов идёт медленно, зато без блокировок. Полный
# первый проход ~30–40 мин; это норма (надёжность важнее скорости).
PAGE_DELAY_SEC = (20, 45)     # пауза между страницами одного региона
REGION_DELAY_SEC = (35, 70)   # пауза между регионами (спокойнее темп = меньше банов)
# Разовый глубокий прочёс — быстрее (он одноразовый, авто-восстановление
# подстрахует от банов). Обычные проходы остаются спокойными (см. выше).
DEEP_PAGE_DELAY_SEC = (12, 22)
DEEP_REGION_DELAY_SEC = (20, 40)
# Разброс старта, чтобы проходы не били строго по расписанию.
START_JITTER_SEC = (0, 20)
# Перемешивать регионы внутри каждого кольца (порядок колец сохраняется:
# сначала нулевое — Ростов/Москва/МО/СПб/Краснодар, затем дальние).
SHUFFLE_WITHIN_RING = True
# Сколько ДАЛЬНИХ регионов проверять за проход (кольцо 0 — всегда, сверх этого).
FARTHER_PER_RUN = 4
# Предел открытий страниц объявлений за один проход (защита от бана аккаунта).
# Что не успели — добираем на следующих проходах.
MAX_DETAIL_FETCHES = 6
# Рекомендуемый интервал запуска в Планировщике: ~15 минут со случайным сдвигом.

# --- Фильтр (наша уникальная логика, которой нет у Авито) ---
PROBA = 585                 # проба золота
MAX_PRICE_PER_GRAM = 8300   # ₽/г: берём объявления дешевле этого порога за грамм
# Ниже этого ₽/г — подозрительно (дешевле лома ~3500–4000): помечаем как возможный
# развод, но всё равно шлём (пользователь просил показывать с предупреждением).
SUSPICIOUS_PRICE_PER_GRAM = 2500
# Ниже этого ₽/г физически невозможно даже для лома 585 (грамм чистого золота
# в 585-м ~2900 ₽): почти наверняка ошибка разбора (код модели принят за вес,
# «105Г» и т.п.). Такие НЕ шлём вовсе, чтобы не мусорить уведомлениями.
MIN_PLAUSIBLE_PRICE_PER_GRAM = 1000

# Только чистое золото — исключать украшения со вставками/камнями (бриллианты,
# фианиты, циркон и т.п.). «Без вставок/камней» при этом не считается наличием.
EXCLUDE_STONES = True
# Минимальный вес изделия, г (нужен вес из описания). 0 — не ограничивать.
MIN_ITEM_WEIGHT_G = 35.0

# --- География и доставка ---
DESTINATION_CITY = "Ростов-на-Дону"  # куда заказываем
REQUIRE_DELIVERY = False             # доставка НЕ обязательна (договариваемся сами)
# Нужны объявления из Европейской части России, не слишком далеко от Ростова.
# Первый грубый срез — блок-лист явно далёких регионов (Сибирь, Дальний Восток,
# зауральские). Регион всё равно показываем в уведомлении; точную «близость»
# позже можно уточнить по расстоянию или через LLM (условие нечёткое).
FAR_REGIONS = [
    # Дальний Восток
    "Приморск", "Хабаровск", "Камчат", "Сахалин", "Магадан", "Чукот",
    "Якут", "Саха", "Амурск", "Еврейск", "Бурят", "Забайкал",
    # Сибирь
    "Новосибирск", "Омск", "Томск", "Кемеров", "Иркутск", "Красноярск",
    "Алтай", "Тыва", "Хакас",
    # Зауралье / Урал (азиатская часть)
    "Свердлов", "Челябинск", "Тюмен", "Курган", "Ханты", "Мансийск", "Ямал",
]

# Разумные границы веса украшения, г — отсекают мусорные срабатывания регулярки.
MIN_WEIGHT_G = 0.1
MAX_WEIGHT_G = 1000.0

# --- Хранилище ---
DB_PATH = "data/seen.db"  # локальная база дедупликации (в .gitignore)

# --- Сбор через API Авито (spfa cookies + curl_cffi) ---
# Ключ сервиса spfa.pro (даёт cookies под наш прокси-IP). Кладём в .env.
SPFA_API_KEY = os.getenv("SPFA_API_KEY", "")
# Прокси одной строкой: http://логин:пароль@host:порт (или socks5://...).
PROXY = os.getenv("PROXY", "")

# --- Авто-восстановление при бане IP + защита от зависаний ---
# Ссылка ротации IP мобильного прокси (mobileproxy). Обязательно с &format=json —
# тогда ответ компактный (без 57КБ HTML). Кладём в .env. Пусто — ротация выключена.
CHANGEIP_URL = os.getenv("CHANGEIP_URL", "")
# Cookies у spfa живут ~12ч и их НАДО переиспользовать (это и есть экономия).
# Освежаем заранее на 11ч — почти впритык к протуханию, но одна покупка в ~11ч
# вместо десятков. Цель: ~2 покупки cookies в сутки в спокойном режиме.
COOKIE_MAX_AGE_SEC = 11 * 3600
# Сколько РЕГИОНОВ подряд должны заблокироваться, прежде чем тратить деньги на
# восстановление. Одиночные/перемежающиеся блоки (рейт-лимит) лечит пауза, не rebuy.
RECOVER_AFTER_BLOCKS = 3
# ПРЕДОХРАНИТЕЛЬ обычного монитора (не регулятор!): при правильной политике ротации
# недостижим. Если сработал — БАГ. 12 покупок ≈ 36 ₽ worst case.
MAX_REBUYS_PER_DAY = 12
# В ГЛУБОКОМ ПРОЧЁСЕ (разовый тщательный сбор бэклога) бюджет выше — пользователь
# осознанно принял трату ради полноты. ~40×3 ₽ ≈ 120 ₽ потолок на прогон/сутки.
DEEP_MAX_REBUYS_PER_DAY = 40
# --- Политика ротации IP (главный регулятор трат: покупка cookies только на новый IP) ---
# Базовый минимум между ротациями. С адаптивным бэкофом короткая база безопасна и
# даёт быстрое восстановление после ЧЕСТНОГО бана.
ROTATE_COOLDOWN_SEC = 240
# Потолок кулдауна при бэкофе (когда IP горят от нашей нагрузки — не бьёмся в стену).
# 10 мин: даже в бэкофе не даём монитору встать на час.
ROTATE_COOLDOWN_MAX = 600
# В РЕЖИМЕ ГЛУБОКОГО ПРОЧЁСА ротируем отзывчиво (короткий кулдаун). Смена IP теперь
# БЕСПЛАТНА (cookies переносятся), так что частые ротации ничего не стоят; кулдаун —
# лишь чтобы уважить лимит смены IP у mobileproxy.
DEEP_ROTATE_COOLDOWN_SEC = 90
# IP прожил дольше этого — нагрузка ок, сбрасываем кулдаун к базе.
ROTATE_LIFETIME_RESET = 2 * 3600
# IP умер быстрее этого — «плохая» раздача из мобильного пула (не наша вина):
# разрешаем переротацию по базовому кулдауну без штрафа-бэкофа.
ROTATE_INSTANT_DEATH = 60
# Минимальный интервал между попытками восстановления (чтобы не спамить spfa при
# сбое). Восстановление можно пробовать много раз за проход — важно для долгого
# глубокого прочёса, который может ловить бан не единожды.
RECOVER_MIN_INTERVAL_SEC = 60
# Потолок восстановлений (смен IP) за процесс. Ротация теперь бесплатна, а долгий
# прочёс может ловить бан много раз — держим высоким (защита лишь от вечного цикла).
MAX_RECOVERIES_PER_RUN = 500
# Минимальный баланс spfa (₽), при котором ещё покупаем cookies. Одна покупка ~3 ₽;
# ниже буфера не ротируем IP (ротация без возможности купить cookies = мёртвая система).
MIN_SPFA_BALANCE = 6.0
# Если сбор «пустой» дольше этого — один тревожный алерт владельцу в Telegram.
ALERT_AFTER_SEC = 2 * 3600
# Файл состояния здоровья монитора (last_ok, last_rotation, alerted).
HEALTH_PATH = "data/health.json"

# --- Telegram (создаётся у @BotFather; кладём в .env, а не в код) ---
TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN", "")
# Получателей может быть несколько — перечисли chat id через запятую в .env:
# TELEGRAM_CHAT_ID=5041766473,123456789
# Каждый получатель должен один раз нажать «Start» у бота, иначе бот не сможет
# ему написать (Telegram не разрешает боту писать первым).
TELEGRAM_CHAT_IDS = [
    c.strip() for c in os.getenv("TELEGRAM_CHAT_ID", "").split(",") if c.strip()
]

# --- Прокси (обход блокировок по IP; резидентный, желательно sticky-session) ---
# Кладём в .env. Пусто — работаем без прокси (как сейчас).
PROXY_SERVER = os.getenv("PROXY_SERVER", "")        # напр. "http://host:port"
PROXY_USERNAME = os.getenv("PROXY_USERNAME", "")
PROXY_PASSWORD = os.getenv("PROXY_PASSWORD", "")

# --- Поведение браузера (проверенная рабочая формула против антибота Авито) ---
# Запускаем НАСТОЯЩИЙ Chrome, а не встроенный Chromium — ключевой фактор.
CHROME_CHANNEL = "chrome"

# Отдельный «прогретый» профиль с cookies (НЕ основной профиль пользователя):
# так проходим антибот, не пачкаем личный Chrome и не рискуем аккаунтом.
# Лежит в data/ (в .gitignore). Первый запуск прогревается через главную.
PROFILE_DIR = "data/chrome-profile"

# headless антибот стал резать (самая палевная конфигурация). Поэтому headed —
# реальный Chrome с окном, но вынесенным ЗА ЭКРАН (OFFSCREEN), чтобы не мешать.
# Антибот видит настоящий браузер, пользователь окна не видит.
HEADLESS = False
OFFSCREEN = True  # при headed — увести окно за пределы экрана

# Прогрев: перед выдачей зайти на главную (накопить/освежить cookies).
WARMUP = True

# Ресурсы НЕ блокируем: блокировка CSS/JS ломала JS-челлендж антибота.
# (Трафик критичен только при резидентном прокси, которого у нас нет.)

# Антибот «мигает» — иногда отдаёт заглушку даже живому браузеру (у пользователя
# лечилось перезагрузкой). Поэтому при блоке перезагружаем страницу N раз.
BLOCK_RETRIES = 2

# Таймаут навигации/ожидания элементов, мс.
NAV_TIMEOUT_MS = 40_000
